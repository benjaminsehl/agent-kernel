# Integrations

Notes about additive local integrations that extend the agent's environment without replacing its core runtime.

Good examples:
- local messaging backends
- notification bridges
- environment-specific automation hooks
- local scripts that support the agent's workflow

The key principle is additive architecture: build extensions, not replacements.
